const express = require("express");
const router = express.Router();
const pkmnEncounterController = require("../controllers/pkmnEncounterController");

// public routes

// protected routes


module.exports = router;