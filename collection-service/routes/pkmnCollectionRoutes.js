const express = require("express");
const router = express.Router();
const teamController = require("../controllers/pkmnCollectionController");

// public routes

// protected routes


module.exports = router;